services:
  - type: web
    name: mediscan-ai
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn mediscan_ai.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11"
